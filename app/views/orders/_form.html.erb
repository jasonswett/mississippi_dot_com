<%= simple_form_for(order, url: check_user_orders_user_sessions_path) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag :customer_email %>
    <%= text_field_tag :customer_email %>
  </div>

  <div class="field">
    <%= form.association :books, as: :check_boxes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
